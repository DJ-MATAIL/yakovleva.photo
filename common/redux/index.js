const imageViewer       = require('./image-viewer')
const layersList        = require('./layers-list')
const sectionImagesList = require('./section-images-list')
const sectionsList      = require('./sections-list')
const slidesList        = require('./slides-list')

module.exports = {
    imageViewer,
    layersList,
    sectionImagesList,
    sectionsList,
    slidesList
}
